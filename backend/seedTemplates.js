// Seeding removed by maintainer: project is Supabase-first.
//
// The previous `seedTemplates.js` populated a local DB variant (SQLite)
// with demo templates. To avoid ambiguity and accidental local/remote
// data divergence, seeding is disabled. Use Supabase as the single
// source of truth and seed via SQL or the `scripts/seedSharedTenant.js`
// helper when necessary (run manually).

console.log('seedTemplates.js has been disabled — seeding is intentionally removed');
process.exit(0);
