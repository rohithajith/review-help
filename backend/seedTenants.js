// Seeding removed by maintainer: project is Supabase-first.
//
// Previous behavior: this script created local tenant DBs and seeded demo
// businesses/templates. To avoid accidental writes to Supabase, the
// repository now disables automatic seeding. If you need to seed a
// development Supabase instance, use the idempotent script at
// `scripts/seedSharedTenant.js` (review and run manually), or execute
// SQL directly in your Supabase dashboard.

console.log('seedTenants.js has been disabled — seeding is intentionally removed');
process.exit(0);
