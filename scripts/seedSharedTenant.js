// scripts/seedSharedTenant.js disabled — project is Supabase-first.
// To avoid accidental writes, the seeding helper is intentionally disabled.
console.log('scripts/seedSharedTenant.js disabled. Seed Supabase manually if required.');
process.exit(0);
